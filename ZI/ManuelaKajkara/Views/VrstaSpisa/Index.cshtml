@model IEnumerable<DAL.EF.VrstaSpisa>

@{
    ViewBag.Title = "Vrste spisa";
}

<h2>Vrste spisa</h2>

<p>
    @Html.ActionLink("Novo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Naziv vrste
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NazivVrste)
        </td>
        <td>
            @Html.ActionLink("Uredi", "Edit", new { id=item.SifraVrste }) |
            @Html.ActionLink("Detalji", "Details", new { id=item.SifraVrste }) |
            @Html.ActionLink("Obriši", "Delete", new { id=item.SifraVrste })
        </td>
    </tr>
}

</table>
